version: '3.8'

services:
  web:
    image: nginx
    ports:
      - "80:80"
#version: '3.8'

#services:
  #trusted-coffee:
    #build: .
    #ports:
      #- "3000:3000"
    #volumes:
      #- .:/app
      #- /app/node_modules
    #environment:
      #- CHOKIDAR_USEPOLLING=true
